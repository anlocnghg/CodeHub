<Page
    x:Class="TSS.Views.PreferencePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TSS.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <!--#region NameOfRegion-->
    <!--#endregion-->

    <Grid Style="{StaticResource rootGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Text="Availability" />

        </Grid>

        <StackPanel Grid.Row="1">
            <TextBlock FontWeight="ExtraBold" Text="Preferences Page"><LineBreak /></TextBlock>

            <TextBlock FontWeight="Bold" Text="This will add the Tester's preferences (like co-tester, day/night shifts, and other choices/preferences)." />
        </StackPanel>



    </Grid>
</Page>
